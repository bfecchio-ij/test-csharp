@model IEnumerable<teste_csharp.Models.CandidateExperience>

@{
    Layout = null;
    //ViewData["Title"] = "Index";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Job)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IdCandidate }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdCandidate }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdCandidate })
                </td>
            </tr>
        }

    </table>
</body>
</html>